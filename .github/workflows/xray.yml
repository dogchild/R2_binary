name: Deploy Xray Binaries to R2

on:
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      # 步骤 1: 检出仓库代码
      - name: Checkout repository
        uses: actions/checkout@v4

      # 步骤 2: 安装依赖工具 (现在只需要 unzip)
      - name: Install Unzip
        run: |
          sudo apt-get update
          sudo apt-get install -y unzip

      # 步骤 3: 安装并配置 rclone
      - name: Install and Configure rclone
        run: |
          echo "Installing rclone..."
          curl https://rclone.org/install.sh | sudo bash
          
          echo "Creating rclone config directory..."
          mkdir -p ~/.config/rclone
          
          echo "Writing rclone config file..."
          echo "[myR2]
          type = s3
          provider = Cloudflare
          access_key_id = ${{ secrets.R2_ACCESS_KEY_ID }}
          secret_access_key = ${{ secrets.R2_SECRET_ACCESS_KEY }}
          endpoint = https://${{ secrets.R2_ACCOUNT_ID }}.r2.cloudflarestorage.com" > ~/.config/rclone/rclone.conf

      # 步骤 4: 下载并上传 AMD64 架构的 Xray
      - name: Download and Upload AMD64
        run: |
          echo "Processing AMD64 version..."
          curl -L -o xray-linux-64.zip https://github.com/XTLS/Xray-core/releases/latest/download/Xray-linux-64.zip
          unzip -o xray-linux-64.zip xray
          echo "Uploading to gfw bucket..."
          # 上传原始的 xray 文件, 不再是压缩过的
          rclone copyto --s3-no-check-bucket xray myR2:gfw/xray --progress
          rclone copyto --s3-no-check-bucket xray myR2:gfw/front --progress

      # 步骤 5: 下载并上传 ARM64 架构的 Xray
      - name: Download and Upload ARM64
        run: |
          echo "Processing ARM64 version..."
          curl -L -o xray-linux-arm64.zip https://github.com/XTLS/Xray-core/releases/latest/download/Xray-linux-arm64-v8a.zip
          unzip -o xray-linux-arm64.zip xray
          echo "Uploading to gfw-arm bucket..."
          # 上传原始的 xray 文件
          rclone copyto --s3-no-check-bucket xray myR2:gfw-arm/xray --progress
          rclone copyto --s3-no-check-bucket xray myR2:gfw-arm/front --progress
          
      # 步骤 6: 刷新 Cloudflare 缓存
      - name: Purge Cloudflare Cache
        run: |
          echo "Purging Cloudflare cache..."
          curl -X POST "https://api.cloudflare.com/client/v4/zones/${{ secrets.CF_ZONE_ID }}/purge_cache" \
               -H "Authorization: Bearer ${{ secrets.CF_API_TOKEN }}" \
               -H "Content-Type: application/json" \
               --data '{
                 "files": [
                   "https://amd.dogchild.eu.org/xray",
                   "https://arm.dogchild.eu.org/xray",
                   "https://amd.dogchild.eu.org/front",
                   "https://arm.dogchild.eu.org/front"
                 ]
               }'
